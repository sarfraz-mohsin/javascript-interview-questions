let arr = [1, 2, 3, 4];

let str = "hello";

let obj = {
  name: "sareen",
  age: 20,
};

let num = 5;

console.log(typeof num);

//isArray() to check type of array because there are no arrays in js it will show object if used typeof on an array

let test = Array.isArray(arr);
